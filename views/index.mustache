<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>MinhaDieta</title>
    <link rel="icon" type="image/x-icon" href="/imgs/dieta.png">
    <link rel="stylesheet" type="text/css" href="stylesheet/estilo.css">


    <script type="module">

        const botao = document.getElementsByClassName("entrar");

        botao[0].addEventListener('click', () => {
            const email = document.getElementById("email").value;
            const senha = document.getElementById("senha").value;

            fetch('/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    
                    
                },
                body: JSON.stringify({ email, senha }),
            }).then((response) => {
                if (response.ok) {
                    console.log('Usuario logado com sucesso');
                    window.location.href = "http://localhost:3000/inicial";

                } else {
                    console.log('Erro ao logar');

                }
            })
                .catch((error) => {
                    console.error('Erro:', error);

                });
        })

        const botaoCriarConta = document.getElementsByClassName("CriarConta");

        botaoCriarConta[0].addEventListener('click', () =>{
            window.location.href = "http://localhost:3000/usuario/cadastrouser";
        })


    </script>
    

</head>

<body class="flex-container">

    <header class="flex-header">

        <div class="logo">
            <img class="dieta" src="/imgs/dieta.png" alt="">
            <span>MinhaDieta</span>
        </div>

        <div class="flex-button">
            <button class="CriarConta">Criar Conta</button>
        </div>
    </header>

    <main class="flex-main">

        <div class="resumo">
        <p>O MinhaDieta tem como objetivo facilitar o cadastro de uma refeição para pessoas que queiram melhorar seus hábitos alimentares.</p>
        </div>


        <div class="form">
            <div class="flex-label">
                <label for="email">E-mail</label>
                <input type="email" name="email" id="email">
            </div>

            <div class="flex-label">
                <label for="senha">Senha</label>
                <input type="password" name="senha" id="senha">
            </div>

            <div class="flex-button-entrar">
                <button type="button" class="entrar">Entrar</button>
            </div>

        </div>

        
    </main>

    <footer class="flex-footer">
        <a href="http://localhost:3000/desenvolvedor">Desenvolvedor</a>
        <a href="http://localhost:3000/contato" >Contato</a>
        <a href="http://localhost:3000/sobre" >Sobre</a>
        <a href="http://localhost:3000/tecnologia" >Tecnologia</a>
    </footer>

</body>

</html>
